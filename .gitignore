# Large model checkpoints downloaded from WandB (see CLAUDE.md for how to re-fetch)
prev_paper/models/

# New 800k-step decompositions (re-downloadable, see new-decomps section in CLAUDE.md);
# keep READMEs/configs/SHA256SUMS tracked, ignore archives + checkpoint payloads
new-decomps/*.tar.zst
new-decomps/*/pretrain_cache/
new-decomps/*/runs/*/ckpts/

# sink-models (task-1423 untied-head attention-sink bundles, re-downloadable; see CLAUDE.md);
# keep README/SHA256SUMS/configs tracked, ignore archives + checkpoint payloads
sink-models/*.tar.zst
sink-models/pretrain_cache/
sink-models/runs/*/ckpts/
sink-models/runs/*/harvest/

# Clones of the paper's code library (separate git repos, not ours)
prev_paper/param-decomp/
prev_paper/param-decomp-vpd/
sink-models/param-decomp-sink/

# Cached training-data downloads (re-fetched by token_table.py)
data/

# context-loss sub-project: cached data + per-token result arrays (regenerated by compute.py)
context-loss/hide/cache/
context-loss/hide/results/

# component-analasys sub-project: cached per-component data (regenerated by compute.py)
component-analasys/hide/cache/

# token-embeds sub-project: cached final-residual-stream activations (regenerated by final_acts.py)
token-embeds/hide/cache/

# endoftext-pos0 sub-project: large cached activations (regenerated by pos0_probe.py)
endoftext-pos0/hide/cache/pos0_probe_acts.npz

# Python
__pycache__/
*.pyc
.venv/

# compare-decomps: Modal-derived caches + the widget's sidecar data folder
# (both rebuildable: caches via hide/*_modal.py from volume vpd-4layer,
# hide/data/ via hide/build.py; hide/data/ is already covered by the global
# data/ rule but listed here for clarity)
compare-decomps/hide/cache/
compare-decomps/hide/data/

# Regenerable sub-project caches (2026-09-16, after a failed 2.9 GB push:
# each is rebuilt by the scripts next to it, mostly from Modal volume vpd-4layer)
C-pos-0/hide/cache/
coci-heatmaps/hide/cache/
cos-sim/read-in/hide/cache/
cos-sim/write-out/hide/cache/
endoftext-pos0/hide/cache/
eos-isolation/hide/cache/
hacker-news-hyfen/hide/cache/
mean-ci-widget/hide/cache/
pile-att-L0/hide/cache/
pile-qk-comps/hide/cache/
sink-models/hide/cache/
sink-harvest-widget/hide/cache/

# Exceeds GitHub's 100 MB file limit; rebuild via coci-heatmaps/hide/interactive_cross.py
coci-heatmaps/old-newA-newB/interactive_newA_newB.html

# attention-patherns sub-project: cached L0 attention stats (regenerated by compute.py, ~2 min)
attention-patherns/hide/cache/
