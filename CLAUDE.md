# CLAUDE.md — 4LayerLM

## Project purpose

Investigate the model studied in the previous paper, described in [prev_paper/prev_paper.md](prev_paper/prev_paper.md):
*"Interpreting Language Model Parameters"* (Goodfire/MATS, published May 5th 2026; the user, Linda Linsefors, is a co-author). The paper introduces **adVersarial Parameter Decomposition (VPD)**, a parameter-decomposition interpretability method, applied to a small language model: **67M parameters, 4 layers, trained on the Pile**.

Relevant external resource: the paper's code library — https://github.com/goodfire-ai/param-decomp

## Standing instructions

- **Keep this file up to date.** All Claude instances working on this project should continuously update this file with whatever seems useful about the state of the project (goals, decisions, findings, file layout, gotchas, environment details).
- **Use Modal.** The user has Modal access. Run compute on Modal whenever that is faster than running locally.
- **Code style:** write code that is both fast to run and easy for the user to read.
- **Folder layout (user decision 2026-08-27).** Paper-related artifacts (`prev_paper.md`, `models/`, `model_def.py`, `NewGELU/`) live in `prev_paper/`. Each sub-project folder (the hyphenated names) keeps only human-readable outputs (`.md`, `.png`, `.html`, `.csv`, `.txt`) at top level (plus `simple-emb-clusters/figures/`, which is PNG-only); everything else — scripts, caches, binary/JSON data, `cache/`, `results/`, `data/`, `review/` — lives in `<sub-project>/hide/`. Scripts in `hide/` write human-readable outputs one level up (`HERE.parent`) and keep caches beside themselves; project root for `sys.path`/`ROOT` is now three `.parent`s up from a script in `hide/`. **All future sub-projects must be structured the same way from the start**: create `<sub-project>/hide/` and put code, caches, and anything else not meant for the user to view directly in there; only the human-readable outputs go at the sub-project's top level. File paths in the per-sub-project sections below predate the move: prepend `hide/` for anything not in the keep-visible extensions list.
- **Math:** the user knows math well (physics background). When something can be explained with equations — in chat or in reports — use equations rather than only prose.
- **Token categories (user decisions 2026-08-27).** Not to be confused with alive *subcomponents*.
  - **simple_2l** (old terminology kept): *alive* = `freq` ≥ 10 in `simple-token-table/token_table.csv` (3,702/4,019, 7.9% dead); *dead* = the rest (dead-but-seen vs never-seen where relevant). If the user hasn't specified, ask whether to include all tokens or just alive ones.
  - **pile_4l** (three named categories; counts over the 4,000 cached rows in `context-loss/hide/cache/pile_rows.pt`, 2.052M tokens): **frequent** = count ≥ 7 (24,437; the half/half cut, chosen for lack of a principled one — noisy for borderline tokens, refetch more rows if results look sensitive); **missing** = the 304 tokens verified to be essentially absent from the *training data* itself (count 0 AND projection > 0.68 onto frequent-PC1; ids cached in `token-embeds/hide/missing_tokens.npy` — very long whitespace runs, all-caps fragments, mojibake, all at the push-away cone equilibrium PC1 ≈ 0.71); **unfrequent** = everything else (25,536). **Missing tokens are excluded from all pile analyses by default.** If the user hasn't specified, ask whether to include all non-missing tokens or just frequent ones.

## Background: the model & method (from prev_paper.md)

- VPD decomposes each weight matrix into rank-one *subcomponents* $W_l \approx \sum_c \vec{U}^l_c (\vec{V}^l_c)^\top = U^l (V^l)^\top$, possibly overcomplete (more subcomponents than the matrix rank), to capture mechanisms in superposition.
- Subcomponents are trained to be parameter-faithful (sum to $\theta$), minimal, mechanistically faithful (robust to adversarially chosen ablations of unused subcomponents — the key difference from SPD's stochastic ablations), and simple.
- The paper builds attribution graphs between subcomponents ("circuits"), decomposes attention into cross-head subcomponents, and demonstrates model editing via parameter subcomponents.

## Artifact locations (from prev_paper.md appendix)

All model artifacts are on WandB (accessible via the logged-in `wandb` Python API):

Local layout: `prev_paper/models/<model>/target_model_<run>/` + `prev_paper/models/<model>/vpd_decomposition_<run>/` — each decomposition sits next to the model it decomposes.

**Main model (Pile, 4-layer), in `prev_paper/models/pile_4layer/`:**

- **Target model** — WandB run `goodfire/spd/t-9d2b8f02`. Files: `model_step_99999.safetensors` (268 MB; also a `.pt` on WandB), `model_config.yaml`, `final_config.yaml`, `tokenizer.json`.
  - Architecture (`model_config.yaml`): `LlamaSimpleMLP`, 4 layers, `n_embd=768`, 6 heads (GQA with 6 KV heads, i.e. effectively MHA), `n_intermediate=3072`, `n_ctx=512`, rotary (dim 128, base 10000), RMSNorm, no biases, vocab 50277 (GPT-NeoX/Pile tokenizer).
- **VPD decomposition (the subcomponents)** — WandB run `goodfire/spd/s-55ea3f9b`. Files: `model_400000.pth` (2.9 GB), `final_config.yaml`.

**SimpleStories model (2-layer), in `prev_paper/models/simplestories_2layer/`:**

- **Target model** — WandB run `goodfire/spd/gf6rbga0` (found via `pretrained_model_name` in the decomposition's `final_config.yaml`). Files: `model_step_99999.pt` (63 MB), configs, `tokenizer.json`.
  - Architecture: same family, 2 layers, `n_embd=192`, 6 heads (3 KV heads), `n_intermediate=768`, `n_ctx=512`, rotary dim 32, vocab 4019 (tokenizer `SimpleStories/test-SimpleStories-gpt2-1.25M`).
- **VPD decomposition** — WandB run `goodfire/spd/s-eab2ace8`. Files: `model_400000.pth` (98 MB), `final_config.yaml`. Subcomponent counts per matrix (from config): `mlp.c_fc` C=1152, `mlp.down_proj` C=960, `attn.q_proj`/`k_proj` C=288, `v_proj` C=384, `o_proj` C=480.
- **Other runs referenced in the paper** (not downloaded; see paper appendix ~line 2240): all decompose the same `t-9d2b8f02` target unless noted.
  - VPD multiseed: 5 seeds of the main decomposition (seed-consistency table).
  - Capacity sweep: 0.5×/1×/2×/4× subcomponent counts (feature-splitting analysis; 1× = main run).
  - No-adversarial-loss control `s-05ef623e`: same config minus the adversarial ablation loss (≈ SPD-style).
  - Hidden-act aux-loss VPD `s-aa4fec0a`: adds stochastic-forward-pass hidden-activation MSE loss.
  - PLT/CLT baselines: per-layer & cross-layer transcoders, BatchTopK k∈{8,16,32,64}, 4k/32k dicts, local-MSE and end-to-end-KL objectives (`mats-sprint/pile_*` WandB projects).
  - SimpleStories `s-eab2ace8`: a *different* 2-layer model (same architecture) trained on SimpleStories + its VPD decomposition; cleaner components, narrower distribution — good sandbox.
- **Code:** the paper's library is cloned at `param-decomp/` (https://github.com/goodfire-ai/param-decomp) — use it to load the checkpoints. Main package: `param_decomp/`; there is also `nano_param_decomp/`.

## Checkpoint file formats

All checkpoints are plain PyTorch **state dicts** (named tensors), not pickled model objects — instantiate the architecture from `param-decomp` (or read tensors directly).

- Target checkpoints: flat state dict of `LlamaSimpleMLP` (`wte.weight`, `h.<l>.attn.{q,k,v,o}_proj.weight`, `h.<l>.mlp.{c_fc,down_proj}.weight`, `h.<l>.rms_{1,2}.weight`, ...).
- Decomposition checkpoints (`model_400000.pth`) contain three key groups:
  1. `target_model.*` — frozen copy of the target weights (self-contained file).
  2. `_components.<module>.{U,V}` — the subcomponent factors; subcomponent $c$ is rank-one $\vec V_c \vec U_c^\top$ (column $c$ of $V$, row $c$ of $U$); $VU$ reconstructs the weight matrix. Shapes e.g. SimpleStories `h-0-mlp-c_fc`: V (192,1152), U (1152,768).
  3. `ci_fn.*` — the causal-importance function: a small 4-block transformer (d=512) whose output head emits one importance value per subcomponent (7104 total for SimpleStories). Used to decide which subcomponents are active per token.

Architecture comparison between the two models: see `architecture_comparison.md`.

## Loading the models

Use `load.py` at the project root — **standalone, runs on the user's default Python 3.11** (only needs torch/yaml/safetensors). `load_pile_4l()` / `load_simple_2l()` / `load(name)` return `(model, parameter_components, tokenizer)` (tokenizer is a transformers `PreTrainedTokenizerFast` from the local tokenizer.json; also available alone via `load_tokenizer(name)`):

- `model`: a `LlamaSimpleMLP` from `prev_paper/model_def.py` (a 3.11-compatible port of the paper repo's model file; verified **bit-exact** against the library forward pass on identical inputs).
- `parameter_components`: a `ParameterComponents` with `.components["h.0.mlp.c_fc"].V` (d_in, C) and `.U` (C, d_out) — subcomponent c is the rank-one matrix V[:,c] U[c,:], and W ≈ (V U)ᵀ in nn.Linear orientation (`.reconstruct(module)` does this; relative faithfulness error ~1e-3). CI-function weights kept raw in `.ci_fn_state_dict`.

**When the 3.13 venv is still needed:** the `param_decomp` library's `ComponentModel` machinery (masked/ablated forward passes, evaluating the causal-importance function, sampling) — it can't run on 3.11 (uses 3.12+ typing). For that, run with `param-decomp-vpd\.venv\Scripts\python.exe` (venv created with `uv sync --frozen --no-dev` in `param-decomp-vpd/`) and use `ComponentModel.from_pretrained(<local .pth path>)`; on first use it fetches target weights from WandB into `~/param_decomp_out/`.

**Repo folders:** `param-decomp/` = main branch (JAX rewrite); `param-decomp-vpd/` = git *worktree* of it at the `vpd-paper` release tag (the Torch code matching the checkpoints) — don't delete `param-decomp/`, its `.git` holds both.

**Decision (2026-08-24): use the Torch (`vpd-paper`) version for this project.** The JAX rewrite on `main` (`param-decomp/`) is a possible future option worth keeping in mind: `param_decomp/targets/llama_simple_mlp.py` reimplements the same target model on a shared GLU-transformer engine, loads the same `t-9d2b8f02` weights (converted to safetensors), and is pinned numerically equivalent to the Torch model by test fixtures (`targets/tests/simple_mlp_equivalence/`). Potentially useful for fast large-scale forward-pass sweeps on Modal GPUs. Caveat: its new training/checkpoint format differs, so it cannot load the paper's decomposition checkpoint directly. Also of independent value in `param-decomp/`: `docs/handbook.md` and `docs/skill.md` (methodology guides), `nano_param_decomp/` (compact Torch reference implementation).

**Numerical gotcha:** the models use NewGELU (tanh approximation, GPT-2 style), not exact GELU — max difference ≈ 5e-4 at |x| ≈ 2.7. Any reimplementation of the forward pass must use `F.gelu(x, approximate="tanh")` or the explicit tanh formula. See `prev_paper/NewGELU/plot_activations.py`.

## Training data

- Pile 4-layer model: `danbraunai/pile-uncopyrighted-tok-shuffled` on HF (pre-tokenized shuffled `monology/pile-uncopyrighted`, GPT-NeoX tokenizer, 513-token rows with `<|endoftext|>` document boundaries mid-row).
- SimpleStories 2-layer model: `SimpleStories/SimpleStories` on HF (LLM-generated children's stories, `story` column + style/topic metadata columns).
- `load.py` also fetches sample rows from both via the public datasets-server REST API (no HF login). `pile_samples(n, offset)` / `simple_samples(n, offset)` return a list of 1-D int64 token-id tensors: Pile rows passed through as-is (already tokenized, exactly as in training), stories encoded with the simple_2l tokenizer without the appended [EOS]. Decode with `load_tokenizer(...).decode(ids.tolist())`.

## Alive components & harvest data

- Per-matrix alive-subcomponent counts (paper definition: mean CI > 1e-6 on data ≈ "non-negligible CI on ≥1 token per ~1M tokens"): tables in `architecture_comparison.md`. Pile table (updated 2026-08-26): exact counts from harvest.db mean CI — 9,973/38,912 alive (26%), matches the paper's per-layer table except one extra layer-0 component (paper total 9,972). SimpleStories table: estimated (2026-08-25) via `ComponentModel` + `calc_causal_importances(...).lower_leaky` over 100 training sequences — 6,499/7,104 (91%); the harvest DB's `mean_ci` gives 6,782 (100-seq method undercounts rare components by ~1.4% on Pile, more here).
- The paper's full active-component list + LLM labels come from the `pd-harvest`/`pd-autointerp` pipeline (`harvest.db`: per-component stats + activating examples; `interp.db`: LLM labels). Not on WandB/in the repo — **received from a co-author 2026-08-25**, now local under `prev_paper/models/<model>/additional-component-data/` (see next section). Rerunning from scratch would need the harvest worker (`python -m param_decomp.harvest.scripts.run_worker`) + an OpenRouter key for autointerp.
- The WandB decomposition runs do log per-matrix mean L0 (`train/l0/*`, `eval/l0/*` in the run summary) — exact values matching the paper's L0 column.
- Gotcha: `pile_samples` rows are 513 tokens but n_ctx = 512 — truncate to 512 before a forward pass.

## Harvest & autointerp databases (received 2026-08-25 from a co-author)

SQLite DBs in `prev_paper/models/<model>/additional-component-data/` (gitignored with the rest of `prev_paper/models/`); each folder has a `README.txt` from the sender. Both harvests: 20,000 batches × 32 seqs, 1,000 activation examples/component, 20 context tokens/side, top-40 token PMI. Verified 2026-08-25: SimpleStories archive SHA-256 matches; all row counts match the READMEs.

**Pile 4-layer** (`prev_paper/models/pile_4layer/additional-component-data/`), for decomposition `s-55ea3f9b`:
- `harvest.db` (**51 GB**, decompressed from `harvest-h-20260227_010249.db.zst`, 8 GB — both kept; delete one if space is needed). Tables: `components` (38,681 rows = every subcomponent that fired ≥once at activation threshold 0; 231 of 38,912 never fired), `scores` (9,147 intruder scores, avg 0.419, per-trial LLM reasoning included), `config`. Component columns: `component_key` (`h.<l>.<matrix>:<idx>`), `layer`, `component_idx`, `firing_density`, `mean_activations` (JSON: `causal_importance`, `component_activation`), `activation_examples` (JSON: ~1000 × {token_ids, firings, activations{causal_importance, component_activation}}), `input_token_pmi`/`output_token_pmi` (JSON top-40 [token_id, pmi]), `n_activation_examples`. Sum of mean-CI>1e-6 per matrix ≈ 9,973 — matches our 9,837 estimate to ~1.4%.
- `interp.db` (decompressed from `autointerp-a-20260318_115137.db.zst`): `interpretations` — 38,681 rows (full coverage of harvest), columns `component_key, label, confidence, reasoning, raw_response, prompt`. Confidence: 5,294 high / 15,711 medium / 17,676 low; only 119 labeled unclear-ish. `scores` table empty (Pile autointerp was not detection/fuzzing-scored).
- Note: the Pile README mentions SHA-256 sidecars but none were included — flagged, not blocking. Optional `component_correlations.pt` (12.1 GB) and `token_stats.pt` (23.5 GB) exist on the cluster but were not sent (ask if needed).

**SimpleStories 2-layer** (`prev_paper/models/simplestories_2layer/additional-component-data/`), for decomposition `s-eab2ace8`, extracted from the `.tar.zst` (kept):
- `harvest/s-eab2ace8/h-20260212_142914/harvest.db` (8.3 GB): `components` — 7,049 rows (of 7,104 total), columns as Pile but older schema: `mean_ci` REAL directly (no firing_density/mean_activations JSON) and activation examples as {token_ids, ci_values}. 6,782 have mean_ci > 1e-6 (vs our 6,499 estimate). `scores`: 2,821 intruder (avg 0.359).
- `autointerp/s-eab2ace8/a-20260212_142914/interp.db` (+ its `-wal`): `interpretations` — 6,477 rows; confidence 2,811 high / 2,519 medium / 1,147 low; 1,186 unclear-ish labels. `scores`: 6,476 detection (avg 0.553) + 6,474 fuzzing (avg 0.604).

Usage notes: token ids in examples/PMI are in each model's own tokenizer; open DBs read-only (`sqlite3.connect(f"file:{path}?mode=ro", uri=True)`); label lengths are short free-text (e.g. "punctuation preceding interjections"); the autointerp `prompt` column stores the full LLM prompt (~9–13 kB) per component.

## SimpleStories token table

`simple-token-table/` holds `token_table.csv` (one row per token id), `sorted_token_table.csv` (same rows sorted by class → top_pos → freq desc), `README.md` (column documentation — read it before using the table), and the build script `token_table.py` (~5 min, needs spaCy `en_core_web_sm` — installed 2026-08-25; story cache in `simple-token-table/hide/data/`, gitignored). Classifies all 4,019 SimpleStories tokenizer tokens:

- `class` (mutually exclusive): `word` 2427 (standalone in the **majority** of occurrences: standalone_count/freq > 1/2) / `prefix` 762 (mostly the first piece of longer words — merge intermediates like `adventur`, plus minority-standalone words like `sleep`, `t`, `ben`) / `suffix` 798 (`##` continuations) / `punctuation` 20 / `digit` 10 / `special` 2.
- `freq` (corpus token count over 20k stories), `standalone_count` (occurrences as a complete word).
- POS distribution `pos_*` (fractions from spaCy on raw capitalized text; source: the dataset's `test` parquet shard) + `top_pos`. For prefix/suffix tokens the distribution is over words *containing* them; `example_words` lists the top 3 such words.
- `name_frac` = P(capitalized ∧ PROPN | mid-sentence standalone occurrence) — fraction of proper-name usage. `cap_frac` = capitalization alone. Both empty if < 3 mid-sentence occurrences. ~20 pure-name tokens (lily, ben, mia, ...), ~32 dual-use (mom 0.17, earth 0.37, keeper 0.42, fluff 0.83).
- Gotchas: spaCy-sm mis-tags lowercase "awe" as PROPN (why name_frac requires capitalization *and* PROPN); no published classification of this vocab exists (checked paper/repos/HF 2026-08-25). Tokenizer facts: WordPiece (greedy longest-match, `##` = continuation), lowercased, digits split individually, whitespace destroyed, no byte fallback (unknown chars → whole word = `[UNK]`); trained toward 4096 vocab but merges saturated at 4019.

## Analysis code

- `circles.py` (adapted from the user's "Abs in Sup" project): probes local geometry of the residual stream — output displacement along random directions, and Frobenius norms of Jacobians over a random 2-plane grid. Key conventions:
  - Layer coordinates in halves: 0 = post-embedding, 0.5 = after layer-1 attention, 1 = after layer 1, ..., n_layer = after last layer (final RMSNorm applied only when out_layer = n_layer).
  - `layers_fn(net, in_layer, out_layer, sequence=...)` returns the residual-stream map; `sequence=False` treats vectors as length-1 sequences, `sequence=True` runs real causal attention. `text_h0(net, tokens, in_layer)` gives the stream for a tokenized text (token ids as list[int] or 1-D tensor — e.g. `tokenizer.encode(text)` or a `pile_samples`/`simple_samples` row; no tokenizer argument).
  - Defaults everywhere: `sequence=True, vary='last'` (perturb + measure at the last token position only; output side is always the last position in sequence mode).
  - `vary='last'` Jacobians use KV-cached suffix differentiation (prefix keys/values cached without autodiff, jacfwd on a single position) — ~30x faster than naive autodiff and seq-len-independent; verified vs full-sequence jacrev on both models.
  - `text_change(net, tokens, ...)` / `text_jacobian(net, tokens, ...)` are token-input wrappers around the two plot functions.
  - Known corners, all ruled acceptable by the user (2026-08-24) — do not re-flag: `in_layer=n_layer` mixes pre-norm h0 with a post-norm map (irrelevant: only in_layer < out_layer is of interest); each call draws a fresh random plane (intentional); `sequence=True` default silently reinterprets a hand-built (B, d) vector batch as one sequence (acceptable: the main workflow is `text_jacobian`/`text_change`, where h0 is built internally and always consistent); stale demo comment/labels (user: don't worry about it).
- `load_simple_2l()`'s tokenizer is wrapped (`NoSpecialTokens`) so encode does NOT append [EOS]; the bare `load_tokenizer("simple_2l")` still does.

## Context-loss sub-project

`context-loss/` — how per-token loss depends on context length, both models (full writeup: `context-loss/report.md`). `compute.py` writes per-token (loss, within-doc EOS distance) arrays to gitignored `results/*.npz` (~10 min on local GPU, first run also fetches data); `plots.py` makes the figures + power-law fits. Key findings (2026-08-25): loss vs same-document context follows $L(n) \approx L_\infty + A\,n^{-\alpha}$ with $\alpha \approx 0.45$ (Pile, $L_\infty = 2.20$, still gaining at the full 512 window) vs $\alpha \approx 0.78$ (SimpleStories, saturates by $n \approx 100$); out-of-document context (across EOS) is worth ~nothing to either model; cold-open (first-token-of-doc) loss sits at the empirical entropy floor of document openings; loss vs *position in document* is non-monotone (formulaic openings, Pile entropy spike at positions ~4–6). Gotchas: HF datasets-server rate-limits after ~100 requests — the fetcher has backoff + partial caching; mean Pile doc length ≈ 1,450 tokens, so 86% of Pile tokens' documents start before their row (their `dist` is −1 = truncated context).

## Capability-analysis sub-project

`4L-capability-analasys/` — what the pile_4l model can/cannot do, measured per-token (full writeup: `4L-capability-analasys/report.md`). Pipeline: `compute.py` (400 cached Pile rows → 204,800 predictions: loss/rank/entropy/top-10 → gitignored `results/predictions.npz`) → `metrics.py` (accuracy by token class, copy-conditions, calibration, error anatomy) → `pos_check.py` (spaCy in-slot POS on errors) → `probes.py` (hand-built skill probes) → `sample_for_review.py` (340 stratified examples) → 8 parallel LLM judges (outputs in `review/agent_out/`) → `aggregate_reviews.py`. Key findings (2026-08-26): top-1 48.8%, ppl 15.1; copying dominates (85.8% top-1 when the (prev,target) bigram is in context vs 24.5% novel; pure-induction probe 84.8%); near-perfect calibration (ECE 0.007); 76% of errors judged plausible-or-equally-good, only 5% broken; errors preserve spacing 84% / case 90% / class 77%; grammar-agreement minimal pairs 13/14; facts are collocational (Barack→Obama 0.97) not relational (capital-of-France→Paris rank 494); arithmetic absent (2+2=→" -"); characteristic failure modes: safe-generic reflex, "grabby" copying (most salient antecedent, not correct one), template-without-grounding, list-item repetition over advancement. Gotchas: judges saw 60-token windows but the `bigram_seen` flag scans all 512; the high-loss tail is heavily contaminated by corpus noise/tokenization artifacts — don't equate loss with failure.

## Token-embeds sub-project

`token-embeds/` — token-embedding norm analyses, both models. `plot.py` → `embedding_norms.png`: norm histograms (+ pile zoom panel) with the mean init norm $0.02\sqrt{d}$ marked (init: wte ~ N(0, 0.02²); lm_head has `LLMC_SKIP_INIT` and is tied, so this is the only init) and top/bottom-10 norm tokens listed. `missing_tokens.py` → `missing_tokens.npy`: the 304 pile missing-token ids (see Standing instructions). `freq.py` → `freq.png`: scatter of corpus count vs norm (symlog x, count-0 strip) + equal-width-bin count histograms (integer positions 0–13 annotated, cutoffs marked) + count vs frequent/alive-PC1 projection (PC1 is a much cleaner frequency axis than norm: Spearman −0.63 pile / −0.81 simple); pile scatters colored by frequent/unfrequent/missing. `pca.py` → `pca.png` and `pca_interactive.py` → `pca_interactive_{pile_4l,simple_2l}.html` (zoomable): PCA spectrum (PCs always from frequent/alive tokens only) + per-group variance and mean projection along each PC (pile groups: frequent / unfrequent / missing; simple groups: alive / dead-seen / never-seen; sign convention frequent/alive-mean ≥ 0). Findings: pile near-isotropic (50%/90% var at 256/622 of 768 PCs), simple more structured (50/148 of 192); the last PC (simple) / last two PCs (pile) are effective **bias directions** — all-token shared offset with tiny alive variance (mean/std ≈ 8–12) — the no-bias network's constant for downstream layers to read. Key findings (2026-08-27): both models tied embeddings (verified in checkpoints; safetensors dedups the shared tensor — pile file has no `lm_head.weight` key). Norm tracks *specificity*, not raw frequency: highest norms = rare specialized fragments, lowest = frequent interchangeable tokens (names, numbers) at or below init norm; Spearman ρ(count, norm | count>0) = −0.27 (pile) / −0.51 (simple). `freq_direction.py` → `freq_direction.png` + `freq_direction_{pile_4l,simple_2l}.npy` (unit vectors): the best linear frequency direction — ridge regression (λ=0.01 by 5-fold CV) of log count on the raw frequent/alive embeddings; all reported correlations are cross-validated. Findings (2026-08-27): frequency is much better linearly encoded than PC1 suggests — CV Spearman +0.82 pile / +0.88 simple (vs PC1 0.64/0.78), CV Pearson(log count) 0.91 both; the direction is *distributed*, not PC1: cos(w, PC1) = 0.25/0.39, top-10 PCs give only 0.74/0.81, top-100 0.76/0.85, and the visually large w-coefficient on the last (bias) PC is a low-variance artifact (last-10 PCs alone: ρ ≈ 0.1; dropping them costs ~0.02). Residual scatter is partly Poisson count noise (pile counts from only 2.05M tokens), so these are lower bounds on decodability. Unfrequent tokens continue the trend monotonically out of the fit set; missing tokens sit far below it at the cone value. **For removing frequency from embeddings** (decision 2026-08-27): do NOT project out w — it has 57% of its mass in the bias PC(s) (ridge w ∝ Σ⁻¹Cov amplifies low-variance directions) and removing it leaves freq decodable at ρ ≈ 0.8; instead mean-center over alive tokens (kills the bias offset), then project out the covariance direction v_cov ∝ XᵀΔy (≈ PC1: cos 0.97/0.98) — this zeroes all linear covariance with log count, leaving only nonlinear remnants (|ρ| ≈ 0.3). Never-trained tokens don't decay to zero — the tied unembedding pushes them away from contexts (grad p_c·h̃ at every position, Adam-amplified) until weight decay balances: degenerate "rare-token cones" at norm ≈ 0.845 (pile — explains the 0.85 histogram spike) and ≈ 1.61 (simple, incl. [UNK], = the cone from simple-emb-clusters).

**Final-residual-stream activations (2026-08-28):** post-`ln_f` (pre-unembedding) vectors on training data — `hide/final_acts.py` (forward hook on `ln_f`; 200 cached Pile rows ×512 + first 360 cached stories ≈ 102k/100k positions; ~1 min local GPU) → gitignored `hide/cache/final_acts_{model}.npz` (acts + acts_pre [same positions just *before* ln_f] float32 + token_ids/row/pos). Plots mirror the embedding visuals, minus frequency/token-category splits: `final_act_norms.py` → `final_act_norms.png` (2×2 histogram grid: post-ln_f top row, pre-ln_f bottom row, top/bottom-10 positions shown with context; √d = norm-if-all-gains-were-1 is printed, not drawn — user removed the reference line 2026-08-28), `final_act_pca.py` → `final_act_pca.png` and `final_act_pca_interactive.py` → `final_act_pca_interactive_{model}.html` (PCA spectrum of centered acts + mean projection μ·v per PC, sign: mean ≥ 0). Findings (2026-08-28): norms ≈ 3.5–4×√d, so ln_f gains are large (pile median 109.6, range 70–124: low tail = digits/symbols, high end = boilerplate/formatting contexts; simple extremely narrow 47.7–50.0, high end mid-sentence before easy continuations, low end sentence-final periods). Pre-ln_f norms (2026-08-28): pile median 26.0 with a long right tail to 89 — the top of the tail is entirely `<|endoftext|>` positions (EOS carries an outsize residual norm); simple median only 1.04 (range 0.6–1.9), so ln_f amplifies the stream ~47× — its high end is abstract/emotion nouns ('truth', 'sorrow', 'despair'), low end mid-word `##` suffix positions. Activations are far less isotropic than the embeddings: 50%/90% var at 30/400 of 768 PCs (pile; embeddings were 256/622) and 31/127 of 192 (simple). Mean projection concentrates on the top ~10 PCs plus a spike at the **last** PC — a low-variance shared-offset (bias-like) direction in the final stream (mean/std ≈ 10 pile, ≈ 3 simple), but it is NOT the embedding bias direction: cos(act last PC, emb bias-PC span) = 0.04 pile / 0.03 simple. It largely IS the embedding **PC1** (≈ v_cov frequency axis) for pile: |cos(act last PC, emb PC1)| = 0.81 pile / only 0.49 simple (v_cov ≈ same: 0.81/0.43; ridge freq dir w: 0.29/0.15), oriented so the offset boosts frequent tokens — Spearman(per-token logit shift from the offset, log count) = +0.64/+0.59 — a **unigram-prior offset** along the unembedding's frequency axis. Refinement (2026-08-28): the freq axis *itself* is NOT low-variance in either model — acts projected onto emb PC1 have |mean|/std ≈ 3.1/3.3 and variance ranking mid-spectrum (#114 of 768 pile, #98 of 192 simple) — the models actively modulate the freq axis (sharpen/flatten the unigram prior). What's frozen is a *component* of it: in pile a single tiny-λ direction carries 66% of emb-PC1's squared mass (hence the clean last-PC match), in simple that low-variance mass is smeared over the last ~20 PCs (‖proj onto last k act PCs‖ = 0.49/0.67/0.73/0.80 for k=1/5/10/20). Act first PC is freq-unrelated in both (cos with emb PC1 = 0.08/0.04). `resid_stages_interactive.py` → `resid_stages_interactive_{model}.html` (2026-08-28): the residual stream at every stage (after embedding / each attention / each MLP / final norm; same data rows, batchwise-accumulated projections, nothing cached) expressed in the frequent/alive-token embedding-PCA basis — per stage, variance along each emb PC (log, panel 1), |mean projection| (log, panel 2), signed mean projection (panel 3) — panel content/order per user request; stages colored sequentially (Turbo), legend toggles all panels. Stage headlines: pile total variance grows monotonically (0.4 after embedding → 663 after MLP 4 → ×~17 by ln_f's gains); simple stays tiny through the whole stack (total var 0.3–1.0) with ln_f doing essentially all the scaling; mean along emb PC1 moves steadily toward the frequent side (negative under the alive-mean≥0 convention: rarer = positive PC1), reaching −12.1/−8.6 at the final norm — the unigram-prior offset building up layer by layer (pile: mostly written by attention 4). `last_pc_cosines.py` → `last_pc_cosines.png` (2026-08-28): |cos| grid between the per-stage smallest-variance stream directions + emb PC1/PC_last (per-stage covariances accumulated batchwise). Findings: the stream's frozen direction starts as the embedding bias direction (cos with emb PC_last: 0.85/0.98 after embedding, still 0.62/0.86 after attention 1), is destroyed by the first MLP in both models, then (pile) a *different* shared frozen direction persists attn 2→attn 4 (mutual 0.76–0.97, unrelated to both emb PCs), until MLP 4 replaces it with the frequency-axis-ish direction (final-norm last PC: 0.81 emb PC1). Simple mirrors this compressed: MLP 2 & final norm share their last PC (0.97), partially freq-axis (0.40/0.49 emb PC1).

## Project state

- **2026-08-24:** Project started. Located and downloaded the target models and VPD decompositions from WandB (see above); cloned param-decomp repo; built standalone 3.11 loading (`load.py`, `model_def.py`) and residual-stream geometry tooling (`circles.py`). Next steps / specific research questions TBD with the user.
- **2026-08-25, reworked 2026-08-27:** `simple-emb-clusters/` — t-SNE + clustering of the SimpleStories token embeddings (full writeup: `simple-emb-clusters/report.md`; the original all-token raw-embedding version is archived in `archive/simple-emb-clusters-2026-08-27/`). The live version restricts to alive tokens (freq ≥ 10 → n=3,702; drops [UNK]/[EOS]/dead tokens/the rare-token cone), mean-centers, and projects out the frequency direction — two variants (`pc1` / `vcov`, cos 0.983) run side by side (`figures/<variant>/`, `hide/cache/<variant>/`; frequencies now from the 20k-story token table, old 1500-story `cache/freq.npy` retired). Findings (2026-08-27): the variant choice is irrelevant — mean 10-NN overlap 0.94, same-method ARI between variants (0.43–0.81) exceeds the within-variant between-method baseline (0.23–0.53); only asymmetry: after vcov no linear freq decodability remains (CV |ρ|≈0.34 nonlinear remnant) vs weak +0.28 after pc1. De-frequencying deletes the frequency banding and the old k=2 frequency split (max silhouette drops 0.082 → 0.027 — even more continuum-like) while all POS×semantic neighborhoods survive; anisotropy of the alive cloud was entirely the shared mean (pairwise cos 0.26 → 0.00 after centering). New: HDBSCAN in native 192-d, previously 100% noise, now isolates a crisp locative-preposition cluster (in/through/around/under/…, n≈15, both variants) + a broad high-freq core; silhouette-optimal KMeans partitions are now semantic, not frequency splits. Old findings that remain (from the archived version): embeddings tied; rare-token cone (324 never-seen, pairwise cos 0.76); corr(log freq, norm) = −0.56 on the full vocab.
- **2026-08-25:** Received the paper's harvest/autointerp SQLite databases (activating examples + LLM labels for both decompositions) from a co-author; verified, decompressed, and documented them — see "Harvest & autointerp databases" section.
- **2026-08-25:** `context-loss/` — loss vs context length for both models — see "Context-loss sub-project" section.
- **2026-08-25:** `generate-storries/` — qualitative sampling from the simple_2l model: 20 random stories (seed 0, drawn from the first 100k train rows), model prompted with each story's first 20 tokens, sampled at temperature 1.0 (no top-k) until [EOS] or 512 tokens. All 20 ended with [EOS] on their own (mean 212 generated tokens vs 313 original). Files: `generate.py` (batched generation loop, ~30 s on CPU), `stories.md` (side-by-side table), `results.json` (raw texts + token ids).
- **2026-08-26:** `component-analasys/` — per-subcomponent statistics for the Pile 4L decomposition. `compute.py` caches magnitude ‖V_c‖·‖U_c‖ + activity category per component (alive = mean CI > 1e-6 from harvest.db / ever-active-but-not-alive / never-active) to gitignored `cache/magnitudes.npz`; `plot.py` renders overlapping density histograms (`magnitudes_hist.png`). Finding: alive components are markedly larger (median 0.77, tail to ~11) than semi-active (median 0.32) and never-active (median 0.30) ones, which have near-identical distributions. `compute_act.py` + `plot_act.py` (→ `cache/activations.npz`, `activations_hist.png`): distribution of subcomponent activations a_c = ‖U_c‖·(V_c·φ) (paper's definition 2; |a_c| = norm of the component's rank-one write into the layer output) over 51,200 Pile tokens, via forward pre-hooks on the 24 decomposed Linears (local GPU, ~1 min); cache holds per-component log-histograms of |a_c| so any threshold can be applied later. Findings: definition-2 activity barely separates the classes — the typical non-alive component has |a_c| > 0.1 on ~half of all tokens (median exceedance fraction: alive 0.66, semi 0.51, never 0.12; even at θ=1, semi components still exceed on ~3% of tokens on average). Also: within each matrix the non-alive components share an extremely narrow interaction scale (sharp per-matrix RMS spikes, e.g. 1,758 dead h.0.mlp.down_proj components all at RMS(a_c) ≈ 0.018 ± 0.001) — suggests dead components collapse to near-copies; pairwise-cosine check would confirm. `compute_max_ci.py <alive|semi>` + `plot_max_ci.py` (→ `cache/max_ci_{alive,semi}.npz`, `max_ci_hist.png`): max CI ever reached per component, from the per-token CI traces in harvest.db activation_examples (scan ~11 min semi / ~4 min alive; also caches max |a_c| and n_examples). Finding: max CI is cleanly bimodal — alive components essentially all saturate CI = 1 somewhere (1st percentile 0.978; only 64 of 9,973 below 0.9, min 0.12), while semi components cluster tightly around 4e-3 (99% below 0.015, absolute max 0.36) and never meaningfully fire; the two distributions barely overlap (~0.1–0.36). Caveat: most components are at the 1000-example reservoir cap, so the stored max is a lower bound (≈ a high quantile of the per-firing CI distribution).
- **2026-08-27:** `token-embeds/` — embedding-norm histograms + frequency-vs-norm scatters, both models — see "Token-embeds sub-project" section. Also confirmed: both target models trained with AdamW weight decay 0.1 (matrices only, incl. the tied embedding; RMSNorm gains undecayed), no bias parameters anywhere. User decisions: simple_2l "alive" = count ≥ 10; pile_4l uses three categories frequent (count ≥ 7) / unfrequent / missing (304 never-in-training tokens, excluded by default) — see Standing instructions for the ask-before-analysis rules.
- **2026-08-27:** `remove-feq-dir/` — session report on the embedding frequency direction (`report.md`): literature check (PC1≈freq and constant-bias direction both confirmed in prior work), precise definitions of w / v_cov / pc1 and their relation, the distributed-encoding and removal-comparison results (duplicating the CLAUDE.md notes above with full math), the simple-emb-clusters rework summary, and a nearest-neighbor table (10 random frequent/alive tokens per model × {raw, PC1-removed, v_cov-removed} top-10 cosine neighbors; `neighbor_table.py` regenerates). Table verdict: pile neighbors barely change; simple_2l rare-query neighbors visibly clean up (fragments → semantic kin); pc1 vs vcov columns differ only in rank order.
- **2026-08-27:** Reorganized the folder layout (see Standing instructions): created `prev_paper/` (now holds `prev_paper.md`, `models/`, `model_def.py`, `NewGELU/`) and per-sub-project `hide/` folders. Updated all path references: `load.py` (imports `model_def` from `prev_paper/`, `MODELS_DIR` → `prev_paper/models`), the six `token-embeds` scripts (`ROOT` depth, `prev_paper/models/...`, `context-loss/hide/cache/pile_rows.pt`), all sub-project scripts' `sys.path` root inserts (one `.parent` deeper) and visible-output paths (one level up), and `.gitignore`. Verified: everything `py_compile`s, `load_simple_2l()` + forward pass work. `remove-feq-dir/` is work in progress (another Claude) — left untouched.
- **2026-08-28:** `token-embeds/` extended with final-residual-stream (post-ln_f) activation analysis — norm histograms + PCA visuals mirroring the embedding ones; see the paragraph at the end of the "Token-embeds sub-project" section.
- **2026-08-28:** `pile-qk-comps/` — RoPE-wavelength spectra of the pile_4l q/k subcomponents (full writeup: `pile-qk-comps/report.md`). `hide/spectra.py` computes per-component per-plane output energy E_{c,p} = Σ_h (U²_{c,128h+p} + U²_{c,128h+p+64}) (plane p = 0..63 pairs dims (p, p+64) per head, λ_p = 2π·10000^{p/64} tokens; rotation-invariant, summed over heads) and plots row-normalized (F_{c,p}, uniform = 1/64) heatmaps `qk_wavelengths.png` (**alive components only** — user decision 2026-08-28; alive = component-analasys categories; rows sorted by spectral CoM, figure rows sized by layer counts) + per-head energy heatmaps `qk_heads.png` (E_{c,h} = Σ_d U²_{c,128h+d}, row-normalized, uniform = 1/6, sorted by dominant head; cache keys `<mod>|head`) + full 3-way heatmaps `qk_heads_wavelengths.png` (E_{c,h,p} = U²_{c,128h+p} + U²_{c,128h+p+64}, 6 head blocks × 64 planes, uniform = 1/384; cache keys `<mod>|hp`) + read-in cosine matrix `qk_readin_cosine.png` (|cos(V_a,V_b)| over all 655 alive q/k components, |·| because component sign is gauge; within-block hierarchical clustering, average linkage on 1−|cos|; cache keys `<mod>|V`; variant `qk_readin_cosine_ci.png` sorted by descending mean CI instead, harvest.db mean CI cached in `hide/cache/mean_ci.npz`) + alive-mean spectra per layer `qk_wavelengths_mean.png`; cache `hide/cache/energies.npz`. Findings: C = 512 per q/k matrix; alive counts q 111/15/92/36, k 126/48/167/60 (layers 0–3 — layer-1 attention barely uses q/k components). Layer 2 is the outlier: a large block of its alive components concentrates energy in the slow λ ≳ 1–2k ≫ n_ctx planes (which rotate ≪ 2π within the window → near-linear coarse-position readout; 6× short-λ depletion in the layer-2 q mean); all matrices share a mild long-λ tilt; no component anywhere specializes in fast planes. Head view: components are mostly cross-head (dominant-head fraction typically 0.2–0.4 vs uniform 1/6; matches the paper), except layer 2 — **layer-2 head 4 is the position head**: its head-4-dominant components (>0.4) are the slow-plane components (q 25/29, k 23/27; and 23/28 of all slow k components are head-4-dominant), i.e. the coarse-position block and the head-4 block are the same mechanism on both q and k sides. 3-way view: layer 0's long-λ tilt is one shared band (head 4, λ ≳ 630) across nearly all alive components; layer 2's position components spread their slow energy over heads 2–4, roughly disjoint from its head-0/1/5 group. Read-in vectors are near-orthogonal (median |cos| 0.026 ≈ random baseline 0.029, max 0.79, 24 pairs > 0.5 — mostly within L2k and L2k×L3k); clustering resolves the excess into a few tight clusters: ~20 comps in L1k (+ aligned L1q group), one cluster each in L2k and L3k that are mutually aligned across layers, small L2q clusters and L0 pairs; largest block means are within-layer-1 (0.065–0.069); the L2 q×k position blocks do NOT share read-in directions (mean 0.032 ≈ baseline) — q/k position components pair via output planes, not input. Context: both models' RoPE is identical up to head dim (base 10000, split-half pairing, full-head rotary) — simple_2l's 16 wavelengths are exactly every 4th of pile's 64; only planes with λ < 512 (pile p ≤ 30) ever complete a cycle in context.
- **2026-08-25:** Made the project a git repository (branch `main`). `.gitignore` excludes `prev_paper/models/` (3.1 GB checkpoints, re-fetchable from WandB), `param-decomp/` + `param-decomp-vpd/` (clones of the paper repo with their own git history), and `__pycache__`. No GitHub remote yet; `gh` CLI is not installed.

## Environment

- Windows 11, VS Code, PowerShell. Modal available for remote compute.
